// Re-export from shared location — all landing page imports continue working
export {
  measureText,
  measureWidth,
  getTextWidth,
  drawTextGlow,
  drawTextWithOpacity,
  easeOutExpo,
  lerp,
  clamp,
  clearCaches,
} from '@/components/pretext/textRenderer';
